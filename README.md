# Vulnerable GraphQL API

Healthcare management GraphQL API used as a security testbed.

## Status

Work in progress. See `package.json` for installed dependencies.

## Quick start

```
npm install
npm start
```

Server listens on port `4000` by default.
